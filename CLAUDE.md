# Event Scanner Runtime

This is a self-contained event scanner runtime. Read `docs/operating-model.md` before doing any event work — it contains the boot sequence, skill pipeline, and all behavioral rules.

For any scan request, invoke the `event-scanner-run` skill.
