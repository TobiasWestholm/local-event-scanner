# Event Scanner

This is the event scanner skill. Read `.agents/skills/event-scanner/references/operating-model.md` before doing any event work — it contains the boot sequence, skill pipeline, and all behavioral rules.

For any scan request, invoke the `event-scanner` skill.
